"====================Class, static, instance methods======================="
# instance мутод(метод обьекта) - метод, который принимает первым аргументом self(обьект). вызываем инстанс методы у обьекта

